services:
  - type: web
    name: class-news
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ADMIN_KEY
        sync: false
      - key: DATABASE_PATH
        value: /var/data/class_news.db
    disk:
      name: class-news-data
      mountPath: /var/data
      sizeGB: 1
